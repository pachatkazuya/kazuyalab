apiVersion: v1
kind: Service
metadata:
  name: {{ include "kazapp.fullname" . }}
  labels:
    {{- include "kazapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kazapp.selectorLabels" . | nindent 4 }}
